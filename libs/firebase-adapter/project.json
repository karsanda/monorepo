{
  "root": "libs/firebase-adapter",
  "sourceRoot": "libs/firebase-adapter/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/firebase-adapter/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/firebase-adapter"],
      "options": {
        "jestConfig": "libs/firebase-adapter/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
